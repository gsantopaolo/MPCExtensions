<Page
    x:Class="TestApp.TestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="Root" PointerEntered="Root_PointerEntered" PointerMoved="Root_PointerMoved" >
        <Border  x:Name="ThirdPanel" Padding="4, 20"  >
            <StackPanel>
                <TextBox Text="Over 65F" ></TextBox>
                <TextBox Header="Rain" Text="No" ></TextBox>
                <TextBox Header="In Attendance" Text="Clinet and their agent" ></TextBox>
            </StackPanel>
        </Border>
        <Grid x:Name="InkerContainer" Grid.RowSpan="2" Visibility="Collapsed">
            <Grid >
                <Grid >
                    <InkCanvas x:Name="Inker" ></InkCanvas>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>
